import styles from "../styles/Home.module.css";

function Footer() {
  return (
    <footer className={styles.footer}>
      Made with <span className={styles.logo}>🖤</span>
      to Epic
    </footer>
  );
}

export default Footer;
